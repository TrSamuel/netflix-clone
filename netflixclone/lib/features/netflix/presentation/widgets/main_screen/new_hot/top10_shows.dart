import 'package:flutter/material.dart';

class Top10Shows extends StatelessWidget {
  const Top10Shows({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("top10 shows"),);
  }
}
