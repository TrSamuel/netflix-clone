import 'package:flutter/material.dart';

class EveryoneWatch extends StatelessWidget {
  const EveryoneWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("everyone"));
  }
}
